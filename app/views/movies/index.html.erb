<h1><%= pluralize(@movies.size, "Movie") %></h1>

<ul>
<% @movies.each do |movie| %>
<%= link_to movie.title, movie_path(movie) %>
<%= movie.rating %>
<%= movie.total_gross %>
<%= movie.description %>
<%= movie.released_on %>
<% end %>
</ul>